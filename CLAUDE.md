@AGENTS.md

# DSS 계측기 관리 시스템

사내 계측기 76대의 목록·사진·교정 이력·성적서를 관리하고,
교정 기한이 다가오면 메일로 알려 주는 사내 웹사이트.
원래 엑셀 한 파일로 관리하던 것을 옮긴 것이다.

**이어서 작업할 때는 이 파일과 아래 문서를 먼저 읽는다.**

| 문서 | 내용 |
|---|---|
| [REQUIREMENTS.md](./REQUIREMENTS.md) | 무엇을 만드는가 (인터뷰 결과, 1·2·3차 범위, 만들지 않는 것) |
| [DESIGN.md](./DESIGN.md) | 1차 설계 (화면 흐름, 데이터 구조) |
| [docs/DESIGN-2차.md](./docs/DESIGN-2차.md) | 2차 설계 (교정 이력, 성적서) |
| [docs/NOTIFY.md](./docs/NOTIFY.md) | 교정 기한 알림 메일 (설정·스케줄러·cafe24) |
| [docs/BACKUP.md](./docs/BACKUP.md) | 백업과 복구 |
| [README.md](./README.md) | 실행 방법, 명령어 |

---

## 지금 어디까지 됐나

**1차 완료** — 목록(검색·필터·정렬·색상 경고) · 상세 · 등록/수정/삭제 · 한↔일 전환 · 임시 로그인
**2차 완료** — 교정 이력, 성적서 업로드/열람, NAS 성적서 350건 이관, 미등록 성적서 화면
**사진 완료** — 상세 화면에서 본체/부속품 사진 추가·삭제 (2026-08-28)
**내보내기 완료** — 엑셀 내보내기 · 목록 인쇄 (2026-08-28)
**알림 완료** — 교정 기한 메일 알림, 수신자·문구 관리 화면 (2026-08-28)
**백업 완료** — 매일 18:30 자동 (Windows 작업 스케줄러)
**알림 자동 실행** — 매일 09:00 (Windows 작업 스케줄러, 실제 발송은 1일만)

### 다음에 할 일

| | 작업 |
|---|---|
| 3차 | dss-auth 통합 로그인 실제 연결 (포털이 열려야 가능) |

---

## 시작하는 방법

PC를 껐다 켰으면 둘 다 꺼져 있다.

```
"C:\Users\이남준\pgsql\bin\pg_ctl.exe" -D C:\pgdata -l C:\pgdata\server.log start
npm run dev
```

http://localhost:3200 → 임시 로그인에서 「이남준 관리자」

---

## 이 프로젝트에서 지켜야 할 것

- **로그인을 만들지 않는다.** dss-auth 포털에 OIDC 로 위임한다.
  포털이 아직 미완성이라 임시 로그인을 쓰고 있다 (`src/lib/auth/dev-login.ts`, 기본값 꺼짐).
- **물리 삭제를 하지 않는다.** 소프트 삭제 4컬럼 고정. 중요한 행위는 감사 로그에 남긴다.
- **NAS 의 원본은 읽기만 한다.** 성적서·엑셀 폴더를 고치거나 지우지 않는다.
- **파일 경로는 상대경로로 DB 에 저장한다.** 구분자 `/`, 전부 소문자.
  최종 운영은 NAS(Synology, Linux) Docker 다.
- **PowerShell 스크립트를 만들지 않는다.** 배치는 TypeScript + tsx.
- **마이그레이션 적용·데이터 변경·git push 는 사용자 승인 후에** 한다.
- 데이터를 고칠 때는 DB 와 `scripts/seed-data/meters.json` **양쪽**을 고친다.
  (`npm run seed -- --reset` 으로 되돌려도 수정분이 살아남게)

## 알아두면 좋은 것

- 교정 기관은 **BCS** 한 곳. 파일명의 `BNB014` · `BCC260626` 은 BCS 가 매기는 파일 고유번호다.
- BCS 가 **2026년 6월부터 성적서 파일명 양식을 바꿔서** 계측기 정보가 파일명에 없다.
  앞으로는 계측기 상세 화면에서 직접 올리는 것이 맞다.
- 엑셀과 인쇄는 **화면 언어를 따른다.** 교산에 보낼 때는 日本語 로 바꾸고 내려받는다.
  둘 다 A4 가로 한 장 폭에 열이 다 들어가게 맞춰 두었다
  (엑셀은 `fitToWidth`, 화면은 `globals.css` 의 `@media print`).
  **열을 새로 넣으면 두 곳 다 폭이 넘치지 않는지 봐야 한다.**
- 알림 메일 문구는 **화면에서 고친다** (`/settings/notify`). 코드에 박지 않는다.
  저장된 것이 없으면 `src/lib/mail/template.ts` 의 기본 문구를 쓴다.
- **cafe24 SMTP 는 587 · STARTTLS 뿐이다.** 465 는 닫혀 있고, 서버가 옛 TLS 라
  `transport.ts` 에서 예외를 열어 두었다. 비밀번호를 바꾸면 반영에 최대 30분.
  안 될 때는 `npm run check-smtp` 부터 돌린다. 자세한 것은 docs/NOTIFY.md.
- 목록 기본 정렬은 `priorityExpression()` — 임박 → 사용중 → 그 외 → 임박·반납 → 기한초과.
  `dueLevel()` 과 같은 판정을 SQL 로 옮긴 것이라 **둘을 함께 고쳐야 한다.**
